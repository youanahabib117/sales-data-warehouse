-- Before running this script, make sure to create the database 'sales_data' and connect to it.

CREATE SCHEMA IF NOT EXISTS raw;
CREATE SCHEMA IF NOT EXISTS relational;
CREATE SCHEMA IF NOT EXISTS analytics;